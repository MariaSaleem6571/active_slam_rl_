.git/
__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/

# Don't ship pre-existing results into the image; they're meant to be
# generated by running the container, and mounted back out via the
# volume in docker-compose.yml. Comment this out if you specifically want
# the demo artifacts baked into the image itself.
results/

native/fs2d/build/
